# Note that these are not for using this dotfiles repository, only for creating flakes for new projects.
{
  rust = {
    path = ./rust;
    description = "A Rust/Cargo project template using nix-community/naersk and oxalica/rust-overlay";
  };
}
