# Global properties
command=/usr/share/i3blocks/$BLOCK_NAME
separator_block_width=15
markup=none

# Volume indicator
[volume]
command=/usr/share/i3blocks/volume 5 pulse
label=Volume
instance=Master
interval=1
signal=10

# Spotify support
[spotify]
command=python3 /home/ivar/.scripts/spotify.py
label=
interval=1

# Memory usage
[memory]
label=Free RAM
separator=false
interval=5

# CPU usage
[cpu_usage]
label=CPU
interval=5
min_width=CPU: 100.00%

[ping_time]
command=ping google.com -c 1 | grep time= | cut -d'=' -f4
label=ping:
interval=1

# Date Time
[time]
command=date '+%H:%M:%S'
interval=1
