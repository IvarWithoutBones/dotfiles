set $mod Mod4

# font for window titles.
font pango:Liberation Sans 10

# exit i3
bindsym $mod+Shift+e exec [ "$(printf "No\\nYes" | dmenu -i -p "Would you like to exit i3?")" = "Yes" ] && i3-msg exit

# restart i3
bindsym $mod+Shift+r exec i3-msg restart

# Mouse+$mod to drag floating windows
floating_modifier $mod

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# resize windows
bindsym $mod+Ctrl+h resize shrink width 10 px or 10 ppt
bindsym $mod+Ctrl+j resize grow height 10 px or 10 ppt
bindsym $mod+Ctrl+k resize shrink height 10 px or 10 ppt
bindsym $mod+Ctrl+l resize grow width 10 px or 10 ppt

# gaps between windows
gaps outer 4
gaps inner 4

# remove titlebar
for_window [class="^.*"] border pixel 1

# split horizontally
bindsym $mod+Shift+Ctrl split h

# split vertically
bindsym $mod+v split v

# change layout
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# enter fullscreen
bindsym $mod+f fullscreen toggle

# toggle floating
bindsym $mod+Shift+space floating toggle

# change focus between floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# kill focused window
bindsym $mod+Shift+q kill

# names for workspaces 
set $ws1 "1"
set $ws2 "2: Media"
set $ws3 "3: Discord"
set $ws4 "4: Spotify"
set $ws5 "5: Games"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10: torrent"

# switch to workspace
bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace $ws5
bindsym $mod+6 workspace $ws6
bindsym $mod+7 workspace $ws7
bindsym $mod+8 workspace $ws8
bindsym $mod+9 workspace $ws9
bindsym $mod+0 workspace $ws10

# move focused program to workspace
bindsym $mod+Shift+1 move container to workspace $ws1
bindsym $mod+Shift+2 move container to workspace $ws2
bindsym $mod+Shift+3 move container to workspace $ws3
bindsym $mod+Shift+4 move container to workspace $ws4
bindsym $mod+Shift+5 move container to workspace $ws5
bindsym $mod+Shift+6 move container to workspace $ws6
bindsym $mod+Shift+7 move container to workspace $ws7
bindsym $mod+Shift+8 move container to workspace $ws8
bindsym $mod+Shift+9 move container to workspace $ws9
bindsym $mod+Shift+0 move container to workspace $ws10

# move workspace to right monitor (wraps)
bindsym $mod+n move workspace to output right

# open applications in specific workspaces (note: you can get the class with xprop)
assign [class="electronplayer"] $ws2
assign [class="discord"] $ws3
for_window [class="Spotify"] move to workspace $ws4
assign [class="Steam"] $ws5
assign [class="Snes9x-gtk"] $ws5
assign [class="hatintimegame.exe"] $ws5
assign [class="Transmission-gtk"] $ws10

bindsym $mod+shift+m exec "i3-msg 'workspace 2: Media; append_layout /home/ivar/.scripts/16:9_layout.json'"

# set colors
set $bg-color 	         #2f343f
set $inactive-bg-color   #2f343f
set $text-color          #f3f4f5
set $inactive-text-color #676E7D
set $urgent-bg-color     #E53935

# window colors
#                       border              	background         text                 indicator
client.focused          $bg-color           	#81848c            $text-color          #81848c
client.unfocused        $inactive-bg-color	$inactive-bg-color $inactive-text-color $bg-color
client.focused_inactive $inactive-bg-color 	$inactive-bg-color $inactive-text-color $bg-color
client.urgent           $urgent-bg-color    	$urgent-bg-color   $text-color          $bf-color

# status bar
bar {
	position top
	font pango:Liberation Sans 9
	status_command i3blocks -c /home/ivar/.config/i3/i3blocks.conf
   		colors {
   			background $bg-color
   			separator #757575
   			#                  border       		background         text
   			focused_workspace  $bg-color			$bg-color          $text-color
   			inactive_workspace $inactive-bg-color 		$inactive-bg-color $inactive-text-color
   			urgent_workspace   $urgent-bg-color   		$urgent-bg-color   $text-color
   		}
}

# volume control
bindsym XF86AudioMute exec amixer set Master toggle
bindsym XF86AudioRaiseVolume exec amixer set Master 5%+
bindsym XF86AudioLowerVolume exec amixer set Master 5%-

# media player control
bindsym XF86AudioPause exec playerctl play-pause
bindsym XF86AudioNext exec playerctl next
bindsym XF86AudioPrev exec playerctl previous
bindsym XF86AudioStop exec playerctl stop

# make second display work
exec_always --no-startup-id xrandr --output HDMI-0 --mode 1280x1024 --pos 0x416 --rotate normal --output DP-0 --mode 3440x1440 --pos 1280x0 --primary

# start notification handler
exec --no-startup-id dunst

# start network manager applet
exec --no-startup-id nm-applet

# start blueman applet
exec --no-startup-id blueman-applet

# set volume to 35%
exec amixer set Master 35%

# map capslock to escape. exec_always since this breaks sometimes.
exec_always --no-startup-id xmodmap -e "remove Lock = Caps_Lock" -e "keysym Caps_Lock = Escape"

# start redshift with the netherlands as an location.
exec --no-startup-id redshift -l 50.77083:3.57361

# launch clipboard manager
exec --no-startup-id clipit

# take screenshot and put into clipboard
bindsym --release Print exec --no-startup-id maim -su /tmp/screenshot.png && xclip -selection clipboard -t image/png < /tmp/screenshot.png

# set wallpaper (note: change with nitrogen)
exec_always --no-startup-id nitrogen --restore

# puts applets on the other monitor (cant find a proper way to do this??)
exec i3-msg restart

# launch terminal
bindsym $mod+Return exec --no-startup-id /home/ivar/.local/bin/st

# lock the screen
bindsym $mod+shift+x exec --no-startup-id /home/ivar/.scripts/lock.sh

# launch dmenu_run (program launcher)
bindsym $mod+d exec --no-startup-id dmenu_run

# launch browser
bindsym $mod+shift+w exec "i3-msg 'workspace 2: Media; exec qutebrowser --qt-flag ignore-gpu-blacklist --qt-flag enable-gpu-rasterization --qt-flag enable-native-gpu-memory-buffers --qt-flag num-raster-threads=2'"

# launch netflix
bindsym $mod+shift+n exec appimage-run /home/ivar/misc/electronplayer.AppImage

# launch discord
bindsym $mod+shift+d exec Discord

# launch spotify
bindsym $mod+shift+s exec spotify

# launch transmission-gtk, my preferred torrent client
bindsym $mod+shift+t exec transmission-gtk
